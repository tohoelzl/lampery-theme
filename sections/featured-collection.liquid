{% comment %}
  Featured Collection Section - Converted from WooCommerce front-page.php Product Sections
  Shopify Native: Uses collection object instead of WP_Query
{% endcomment %}

{%- liquid
  assign collection = collections[section.settings.collection]
  assign products_to_show = section.settings.products_to_show
  assign columns = section.settings.columns
  assign card_size = section.settings.card_size | default: 'medium'
  assign card_gap = section.settings.card_gap | default: 24
  assign container_width = section.settings.container_width | default: 'normal'

  comment
    Columns are now directly controlled by the setting
    Card size only affects visual appearance, not column count
  endcomment
  assign size_columns = columns

  case card_size
    when 'small', 'medium', 'large'
      assign mobile_columns = 2
    when 'extra_large', 'xxl'
      assign mobile_columns = 1
    else
      assign mobile_columns = 2
  endcase

  comment
    Container max-width based on setting
  endcomment
  case container_width
    when 'narrow'
      assign container_max = '1200px'
    when 'normal'
      assign container_max = '1400px'
    when 'wide'
      assign container_max = '1600px'
    when 'extra_wide'
      assign container_max = '1800px'
    when 'full'
      assign container_max = '100%'
    else
      assign container_max = '1400px'
  endcase
-%}

<section id="featured" class="section-lg featured-collection-section" style="--container-max: {{ container_max }};">
  <div class="featured-collection-container">

    {% comment %} Section Header {% endcomment %}
    <div class="section-header">
      <span class="inline-block text-sm font-medium text-primary uppercase tracking-wider mb-3">
        {{ section.settings.subtitle }}
      </span>
      <h2 class="section-title">{{ section.settings.title }}</h2>
    </div>

    {% if collection != blank and collection.products.size > 0 %}
      {% comment %} Products Grid - Using Shopify native product object {% endcomment %}
      <div class="featured-collection-grid featured-collection-grid--{{ card_size }}" style="--card-gap: {{ card_gap }}px;">
        {% for product in collection.products limit: products_to_show %}
          {% render 'product-card', product: product %}
        {% endfor %}
      </div>

      {% comment %} View All Link {% endcomment %}
      {% if section.settings.show_view_all %}
        <div class="text-center mt-12">
          <a href="{{ collection.url }}" class="btn-secondary">
            {{ section.settings.view_all_text | default: 'Alle Produkte ansehen' }}
            {% render 'icon-arrow-right', class: 'w-4 h-4 ml-2' %}
          </a>
        </div>
      {% endif %}
    {% else %}
      {% comment %} Placeholder when no collection is selected {% endcomment %}
      <div class="grid grid-cols-2 lg:grid-cols-{{ columns }} gap-6 lg:gap-8">
        {% for i in (1..products_to_show) %}
          <div class="aspect-product rounded-xl bg-background-alt animate-pulse"></div>
        {% endfor %}
      </div>
      <p class="text-center text-text-muted mt-8">Wähle eine Kollektion in den Theme-Einstellungen aus.</p>
    {% endif %}

  </div>
</section>

<style>
  /* Featured Collection Container */
  .featured-collection-container {
    max-width: var(--container-max, 1400px);
    margin: 0 auto;
    padding: 0 1rem;
  }

  @media (min-width: 1024px) {
    .featured-collection-container {
      padding: 0 2rem;
    }
  }

  /* Featured Collection Grid */
  .featured-collection-grid {
    display: grid;
    grid-template-columns: repeat({{ size_columns }}, 1fr);
    gap: var(--card-gap, 24px);
  }

  /* Mobile */
  @media (max-width: 1023px) {
    .featured-collection-grid {
      grid-template-columns: repeat({{ mobile_columns }}, 1fr);
    }
  }
</style>

{% schema %}
{
  "name": "Produkt-Kollektion",
  "class": "section-featured-collection",
  "settings": [
    {
      "type": "header",
      "content": "Inhalt"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Untertitel",
      "default": "Unsere Favoriten"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Titel",
      "default": "Entdecke unsere beliebtesten handgefertigten Stuecke"
    },
    {
      "type": "header",
      "content": "Kollektion"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Kollektion"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "min": 4,
      "max": 16,
      "step": 4,
      "default": 8,
      "label": "Anzahl Produkte"
    },
    {
      "type": "range",
      "id": "columns",
      "min": 2,
      "max": 6,
      "step": 1,
      "default": 4,
      "label": "Spalten (Desktop)"
    },
    {
      "type": "select",
      "id": "container_width",
      "label": "Container-Breite",
      "options": [
        {
          "value": "narrow",
          "label": "Schmal (1200px)"
        },
        {
          "value": "normal",
          "label": "Normal (1400px)"
        },
        {
          "value": "wide",
          "label": "Breit (1600px)"
        },
        {
          "value": "extra_wide",
          "label": "Sehr breit (1800px)"
        },
        {
          "value": "full",
          "label": "Volle Breite"
        }
      ],
      "default": "normal",
      "info": "Breiterer Container ermoeglicht mehr/groessere Karten"
    },
    {
      "type": "select",
      "id": "card_size",
      "label": "Produktkarten-Groesse",
      "options": [
        {
          "value": "small",
          "label": "Klein"
        },
        {
          "value": "medium",
          "label": "Mittel"
        },
        {
          "value": "large",
          "label": "Gross"
        },
        {
          "value": "extra_large",
          "label": "Sehr gross"
        },
        {
          "value": "xxl",
          "label": "XXL"
        }
      ],
      "default": "medium"
    },
    {
      "type": "range",
      "id": "card_gap",
      "min": 8,
      "max": 64,
      "step": 4,
      "default": 24,
      "unit": "px",
      "label": "Abstand zwischen Karten"
    },
    {
      "type": "header",
      "content": "Button"
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "default": true,
      "label": "\"Alle ansehen\" Button anzeigen"
    },
    {
      "type": "text",
      "id": "view_all_text",
      "label": "Button Text",
      "default": "Alle Produkte ansehen"
    }
  ],
  "presets": [
    {
      "name": "Produkt-Kollektion",
      "category": "Collection"
    }
  ]
}
{% endschema %}
